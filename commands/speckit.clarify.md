# 简体中文输出约束

执行下面的 Spec Kit 核心命令时：

- 所有澄清问题、选项、解释、更新后的需求文本和面向人的总结必须使用简体中文（zh-CN）。
- 不翻译代码、命令、路径、API、标识符、库/框架正式名称和稳定机器标识。
- 保留 `FR-001`、`SC-001`、`US1`、`P1`、`T001`、`CHK001` 等标识。
- 用户明确指定其他语言时，以用户要求为准。
- 对 `spec.md`、`checklists/requirements.md` 的更新必须维持中文文档风格。

{CORE_TEMPLATE}
