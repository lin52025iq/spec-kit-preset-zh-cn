# 简体中文输出约束

执行下面的 Spec Kit 核心命令时：

- 项目宪章的标题、原则名称、原则说明、治理规则、约束和变更说明必须使用简体中文（zh-CN）。
- 保留版本号、日期、文件路径、代码、命令、API、类型/函数/变量名、库/框架正式名称及其他机器可读内容的原始形式。
- 若项目已有英文专有术语，可首次使用“中文名称（English Term）”并保持术语一致。
- 不改变核心命令对宪章同步、治理和版本变更的语义。
- 用户明确要求其他语言时，以用户要求为准。

{CORE_TEMPLATE}
