# 简体中文输出约束

执行下面的 Spec Kit 核心命令时：

- `tasks.md` 的标题、阶段名称、目标、任务描述、依赖说明、并行执行示例和实施策略必须使用简体中文（zh-CN）。
- 严格保留 Spec Kit 的任务协议：`- [ ] T001 [P] [US1] 描述 文件路径`。
- 不翻译 `T001`、`[P]`、`[US1]`、`P1` 等机器可读标识，也不翻译代码、命令、文件路径、API、类型名、函数名、变量名和库/框架正式名称。
- 用户明确要求其他语言时，以用户要求为准。

{CORE_TEMPLATE}
