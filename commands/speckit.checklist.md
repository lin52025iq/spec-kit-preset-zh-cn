# 简体中文输出约束

执行下面的 Spec Kit 核心命令时：

- 所有检查清单标题、类别、检查标准、说明、发现和总结必须使用简体中文（zh-CN）。
- 保留 `CHK001`、`FR-001`、`SC-001`、`US1` 等机器可读标识。
- 不翻译代码、命令、文件路径、API、HTTP 方法、类型/函数/变量名、Schema 字段以及库/框架正式名称。
- `[ ]` / `[x]` 的语义与核心命令保持完全一致，不因本地化改变门禁含义。
- 用户明确要求其他语言时，以用户要求为准。

{CORE_TEMPLATE}
