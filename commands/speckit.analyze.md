# 简体中文输出约束

执行下面的 Spec Kit 核心命令时：

- 一致性分析报告、发现、严重级别说明、建议和总结必须使用简体中文（zh-CN）。
- 引用规格、计划和任务中的机器标识时保持原样，例如 `FR-001`、`SC-001`、`US1`、`T001`。
- 代码、路径、API、命令、类型名、函数名、变量名、库/框架正式名称保持原样。
- 不改变核心分析逻辑、严重级别、门禁规则或修复建议的语义。
- 用户明确要求其他语言时，以用户要求为准。

{CORE_TEMPLATE}
